# AGENTS.md

## 1. プロジェクト概要

Laravelを使用したシフト管理Webアプリである。

現在は、完成済みのスタッフ用閲覧UIを維持しながら、PostgreSQLを利用したデータベース、認証、認可、管理者用UI、下書き編集、自動保存、配布処理を実装している。

将来的には複数企業で利用できるSaaS化を想定するが、現時点では仕様書にない先回り実装を行わない。

---

## 2. 使用技術

- PHP
- Laravel
- Blade
- HTML
- CSS
- Vanilla JavaScript
- PostgreSQL
- Docker Compose
- pgAdmin

以下は追加しない。

- Bootstrap
- Tailwind CSS
- Bulma
- Vue
- React
- Alpine.js
- jQuery
- npmへの新規依存
- フロントエンドビルドを前提とする構成

---

## 3. 仕様の優先順位

作業開始前に、必ず以下を読む。

1. `AGENTS.md`
2. `docs/specs/00_ui_context_map.md`
3. `docs/specs/01_requirements.md`
4. `docs/specs/02_database.md`
5. `docs/specs/03_staff_ui.md`
6. `docs/specs/04_admin_ui.md`
7. `docs/specs/images/`配下の参考画像
8. 現在のコード

依頼内容、仕様書、参考画像、既存コードに矛盾がある場合は、独断で実装を進めない。

以下を報告する。

- 矛盾している内容
- 影響する画面、処理、テーブル、テスト
- 採り得る選択肢
- 推奨案

ユーザーの確認を得るまで、重大な設計変更、Migration実行、既存UIの作り直しを行わない。

### 3.1 仕様書の変更管理

実装中に仕様変更が必要になった場合は、仕様書を変更してはならない。
確定仕様との差分、変更が必要な理由、影響範囲、採り得る選択肢、推奨案を
ユーザーへ報告し、実装を停止する。

セキュリティ上の例外、フレームワークの標準挙動、一般的な実装慣行であっても、
ユーザーの承認を得ずに業務仕様へ追加してはならない。

仕様書には、ユーザーが確定した業務仕様だけを記載する。
次の内容は仕様書へ混在させず、別の作業記録へ記載する。

- 実装上の判断
- 技術的な制約
- 妥協した内容
- 未実装事項
- 実装手順、実装構成、作業経過

実装完了後に、実装結果へ合わせる目的で仕様書を修正してはならない。
実装と確定仕様に差異が判明した場合は、仕様書を正として差分を報告し、
ユーザーの判断を得るまで追加変更を行わない。

仕様書を変更する場合は、変更前に次をユーザーへ報告し、
明示的な承認を得る。

- 元の要件
- 変更内容
- 変更理由
- 影響範囲
- 承認の有無

承認後に仕様書を変更した場合も、完了報告で元の要件、変更理由、
承認を得た事実を示す。

---

## 4. 最重要原則：UIコンテキストを混同しない

本システムには、目的の異なる2つのUIコンテキストがある。

### 4.1 スタッフ用UI

正式名称：

- スタッフ用・マイシフト画面
- スタッフ用・店舗公開シフト画面

原則：

- `/staff/**`配下
- 閲覧専用
- `published_shifts`だけをシフト表示に使用する
- スマートフォン利用を重視する
- 完成済みの既存UIを原則変更しない
- シフト追加、変更、削除、自動保存、配布機能を置かない

### 4.2 管理者用UI

正式名称：

- 管理者用・店舗別シフト編集画面
- 管理者用・スタッフ別シフト確認画面

原則：

- `/admin/**`配下
- PC利用を重視する
- 管理者用・店舗別シフト編集画面は下書き`shifts`を編集する
- 管理者用・スタッフ別シフト確認画面は下書きをスタッフ軸で確認する閲覧専用画面とする
- 自動保存、警告、配布、再配布状態を扱う
- 参考画像と既存Excelの表構造、情報密度、操作感を優先する

### 4.3 禁止される混同

次を行ってはならない。

- 単独で「スタッフ別画面」「店舗別画面」と呼び、文脈を省略する
- スタッフ用Bladeへ管理者用編集機能を条件付きで埋め込む
- 管理者用Bladeをスタッフ用UIとして流用する
- 管理者としてログインしていることを理由に、スタッフ用画面へ下書きを表示する
- スタッフ用画面で管理者用の保存API、配布APIを呼び出す
- 管理者用画面の実装を理由に、`resources/views/staff/**`を無断で変更する
- スタッフ用CSSと管理者用CSSを安易に共通化し、片方の変更が他方へ波及する構成にする

同じユーザーがスタッフ用UIと管理者用UIの両方へアクセスできても、UI、ルート、表示データ、操作権限は混在させない。

---

## 5. ユーザー、権限、ID

現時点では、1人の人物を1件の`users`レコードで管理する。

```text
1人 = 1 users.id
```

権限は以下の3種類とする。

- `staff`
- `shift_manager`
- `system_admin`

`shift_manager`は`staff`を暗黙に含まない。

スタッフ兼シフト管理者には、同じ`users.id`へ次の2ロールを明示的に付与する。

```text
staff
shift_manager
```

ロールとアクセス権は次のように扱う。

| ロール | スタッフ用UI | 管理者用UI |
|---|---:|---:|
| `staff` | 可 | 不可 |
| `shift_manager`のみ | 不可 | 可 |
| `staff` + `shift_manager` | 可 | 可 |
| `system_admin` | 仕様で許可された範囲 | 可 |

認可は、メニューやボタンの非表示だけでなく、Middleware、Policy、Controller、Service等のサーバー側で必ず実施する。

---

## 6. ルート、画面、コードの分離

原則として、次を分離する。

### スタッフ用

- `/staff/**`ルート
- Staff名前空間のControllerまたはAction
- `resources/views/staff/**`
- スタッフ用CSS
- スタッフ用JavaScript
- 公開済みシフトの取得処理

### 管理者用

- `/admin/**`ルート
- Admin名前空間のControllerまたはAction
- `resources/views/admin/**`
- 管理者用CSS
- 管理者用JavaScript
- 下書きシフトの取得、編集、配布処理

共通化してよいのは、UIコンテキストを混同しない内部ロジックに限る。

例：

- Model
- 日付計算
- 時刻計算
- 共通バリデーション規則
- シフト取得Query
- 配布処理のService
- DTO、ViewModelの一部

Blade、CSS、JavaScriptは、見た目や操作目的が異なる場合、共通化より分離を優先する。

---

## 7. スタッフ用UIの保護

スタッフ用画面は完成仕様として扱う。

管理画面の実装中は、原則として以下を変更しない。

- `resources/views/staff/**`
- スタッフ用CSS
- スタッフ用JavaScript
- スタッフ用表示ルート
- スタッフ用Controller
- スタッフ用レスポンシブ挙動
- スタッフ用固定ヘッダー、固定列、スクロール挙動

変更が必要な場合は、実装前に以下を報告し、承認を得る。

- 変更理由
- 対象ファイル
- 画面への影響
- 変更しない代替案

DB接続や認証対応で内部処理を変更しても、見た目、DOM構造、スクロール、操作方法を不用意に変更しない。

---

## 8. 管理者用UIの実装方針

管理者用UIは、一般的な管理画面として独自にデザインし直さない。

参考画像および既存Excelについて、次を忠実に再現する。

- 画面全体の構造
- 表の行列構成
- 情報密度
- 列幅
- 行高
- 罫線
- 余白
- 文字サイズ
- 背景色
- 土日、祝日、当日の見せ方
- 月間計の配置
- シフトコードボタンの位置
- 配布状態と警告状態
- 固定ヘッダー、固定列
- 横スクロール範囲

次を行わない。

- カードUIへの再解釈
- 大きな余白の追加
- 情報の省略
- 列や行の統合
- 独自のダッシュボード化
- 参考画像を背景画像として貼って再現すること
- 大量のインラインCSS

管理者用UIは、最初にダミーデータで静的表示を再現し、ユーザー確認後にロジックを接続する方針を優先する。

---

## 9. シフトデータの原本

シフトデータは共通の原本として管理する。

- 管理者用・店舗別シフト編集画面
- 管理者用・スタッフ別シフト確認画面
- スタッフ用・マイシフト画面
- スタッフ用・店舗公開シフト画面

は、同じ業務データを別の表示軸で表現する。

画面ごとに独立したシフトデータを保存してはならない。

ただし、下書きと公開版は明確に分離する。

```text
管理者用UI = shifts（最新下書き）
スタッフ用UI = published_shifts（最後の公開版）
```

配布後に下書きを変更しても、再配布までスタッフ用UIを変更しない。

---

## 10. 行、列、スタッフ、日付の整合性

画面上の行番号、列番号、配列添字、表示順序だけでシフトを識別してはならない。

データ対応には、少なくとも次を使用する。

- `user_id`
- `store_id`
- `shift_date`
- `shift_id`または`entry_uuid`
- `sequence`

必要に応じてDOMへ次を持たせる。

- `data-user-id`
- `data-store-id`
- `data-shift-date`
- `data-shift-id`
- `data-entry-uuid`
- `data-sequence`

自動保存では、セルの見た目上の位置から保存対象を推測せず、対象IDと実日付を明示して送信する。

スタッフ名、店舗名、シフトコード文字列を識別キーに使用しない。

同姓同名、表示順変更、月切替、絞り込み、複数シフト、削除後の再採番でも、別スタッフ、別店舗、別日付へずれないようにする。

---

## 11. 実装方針

- Laravelの標準的な設計に従う
- Blade、CSS、Vanilla JavaScriptを使用する
- データ取得にはEloquentを使用する
- Controller、Service、Model、Form Request、Policy、Query、ViewModelの責務を分ける
- ControllerへSQL、HTML、複雑な集計、複雑な認可を書かない
- Bladeへ業務ロジックや複雑な日付計算を書かない
- PHPでは可能な限り型宣言を使用する
- JavaScriptのグローバル変数を増やさない
- CSSの色、寸法は可能な限りCSS変数で管理する
- `!important`は原則使用しない
- Seederは既存データと役割を確認してから変更する
- N+1クエリを避ける
- トランザクションが必要な処理では途中状態を残さない
- 一度に大規模な変更を行わない
- 既存機能を壊さないことを優先する
- 仕様外の先回り実装をしない
- 日本語コメントは、意図や理由の説明が必要な箇所に限定して記述する

---

## 12. 自動保存と非同期処理

- 保存ボタンは設けない
- 変更は自動保存する
- 連続変更は仕様に従ってdebounceする
- 保存待ち、保存中、保存済み、保存失敗、競合を表示する
- 保存失敗時にユーザーの入力内容を即座に破棄しない
- 古いレスポンスが新しい入力を上書きしないようにする
- 月切替後に前月のレスポンスを現在の画面へ反映しない
- シフト追加の再試行では同じ`entry_uuid`を使う
- 意図的な別の追加では新しい`entry_uuid`を発行する
- 同じ`entry_uuid`を二重登録しない

---

## 13. 認可とセキュリティ

- URLやリクエストの`user_id`、`store_id`、`shift_id`だけを信用しない
- 操作者が対象店舗、対象スタッフ、対象シフトを扱えるかサーバー側で確認する
- `shift_id`と`user_id`、`store_id`、`shift_date`の関係が不一致なら更新を拒否する
- 所属外または担当外データを取得後に画面上で隠す方式を採らない
- 例外内容、SQL、スタックトレースを利用者画面へ表示しない
- 認証済み画面をユーザー間で共有される公開キャッシュへ保存しない

---

## 14. 禁止事項

- スタッフ用UIと管理者用UIの統合
- スタッフ用UIから下書き`shifts`を表示すること
- 管理者用UIの表示に`published_shifts`を下書き代わりに使用すること
- 画面ごとに別のシフト原本を持つこと
- 表示位置だけに依存した保存
- JavaScriptだけによる権限制御
- 固定スタッフ名、固定店舗名、固定日付配列
- 特定年月専用のHTML
- 未配布時に下書きをスタッフ用UIへ表示すること
- 無断の大規模リファクタリング
- 無断の新規パッケージ追加
- 大量のインラインCSS
- UIを画像として貼り付けて再現すること

---

## 15. Git運用

以下を勝手に実行しない。

- `git commit`
- `git push`
- ブランチの作成、削除、切替
- `git reset`
- `git rebase`
- `git merge`
- `git stash`
- `git clean`
- 履歴や作業ツリーを大きく変更するGit操作

必要な場合は、実行前に以下を説明し、ユーザーの承認を得る。

- 実行するコマンド
- 目的
- 影響範囲
- 元へ戻す方法

変更確認には、原則として読み取り専用の以下を使用してよい。

```bash
git status
git --no-pager diff --stat
git --no-pager diff
git diff --check
```

---

## 16. テスト

既存のテスト方式に合わせ、Feature Testを中心に追加する。

最低限、変更内容に応じて次を確認する。

### 認証・認可

- 未ログイン時の拒否
- `staff`だけでは管理者用UIへアクセスできない
- `shift_manager`だけではスタッフ用UIへアクセスできない
- `staff + shift_manager`は両UIへアクセスできる
- 両UIへアクセスできても、画面とデータ種別が混在しない
- 担当外店舗の閲覧・編集拒否
- IDを直接改ざんしたリクエストの拒否

### スタッフ用UI

- `published_shifts`だけを表示する
- 未配布時に下書きを表示しない
- 管理者権限を持つユーザーでも、スタッフ用UIでは本人の公開シフトだけを表示する
- 既存の見た目、スクロール、レスポンシブ表示を維持する

### 管理者用UI

- 下書き`shifts`を表示する
- 店舗別から入力した内容が、管理者用スタッフ別確認画面へ同じスタッフ、店舗、日付で表示される
- 配布後、同じ内容がスタッフ用UIへ表示される
- 行、列、スタッフ、店舗、日付のずれがない
- 並び順を変えても別スタッフへシフトが移動しない
- 同姓同名でもIDで区別する
- 28日、29日、30日、31日、うるう年を正しく扱う
- 同一日の複数シフトと`sequence`を正しく扱う
- 削除後に他シフトが移動、消失しない
- 非同期レスポンスの逆転で新しい入力が上書きされない

### 配布

- 公開前の変更がスタッフ用UIへ反映されない
- 配布後に公開版へ反映される
- 再配布前は前回公開版を維持する
- 配布処理がトランザクション内で完結する
- 重複勤務や人数不備がある場合は配布を拒否する

テストでは件数だけでなく、`user_id`、`store_id`、`shift_date`、時刻、`entry_uuid`、`sequence`を具体的に検証する。

---

## 17. 完了前チェック

実装完了後は、既存設定と変更範囲に応じて以下を実行・確認する。

- `php artisan route:list`
- PHP構文エラーがない
- Blade描画エラーがない
- 既存テストおよび追加テスト
- フォーマッターや静的解析が既存設定にある場合は実行
- `git diff --check`
- 前月・翌月切替
- 5週・6週カレンダー
- うるう年
- 日本の祝日
- スタッフ用スマートフォン表示
- 店舗別表の横スクロール
- 固定ヘッダーと固定列
- 当日の背景色
- 参考画像との差分
- スタッフ用UIと管理者用UIの混在がない
- 下書きと公開版の混在がない
- 行、列、スタッフ、店舗、日付のずれがない

---

## 18. 完了報告

作業完了時は、以下を報告する。

- 実装した内容
- 変更したファイル
- 変更しなかった保護対象ファイル
- 実行したテストと結果
- 参考画像との差分として残っている点
- スタッフ用UIと管理者用UIを分離した方法
- 使用したデータ種別（`shifts`または`published_shifts`）
- 行列ずれを防ぐ識別キー
- 未解決事項
- 仕様上判断が必要な点
- Git commit、push、stash等を実行していないこと
